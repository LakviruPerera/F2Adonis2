% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/F2_Adonis2_Documentation.R
\name{F2_adonis2}
\alias{F2_adonis2}
\title{Permutational Multivariate Analysis of Variance Using the Robust F2 Statistic}
\usage{
F2_adonis2(
  formula,
  data,
  permutations = 999,
  method = "euclidean",
  sqrt.dist = FALSE,
  add = FALSE,
  by = NULL,
  parallel = getOption("mc.cores"),
  na.action = na.fail,
  strata = NULL,
  bootstrap = FALSE,
  bias.adjust = FALSE,
  ...
)
}
\arguments{
\item{formula}{Model formula. The LHS must be a community data matrix or a
dissimilarity matrix. The RHS must define a single factor predictor.}

\item{data}{A data frame containing the independent variables.}

\item{permutations}{Number of permutations for the test, or a permutation matrix.}

\item{method}{Pairwise distance method used in \code{vegdist}. Common options include
"jaccard" (for binary data), "bray" (for abundance data), or "euclidean".}

\item{sqrt.dist}{Logical; take square root of dissimilarities to "euclidify" them.}

\item{add}{Add a constant to dissimilarities to avoid negative eigenvalues.
Options are "lingoes" or "cailliez".}

\item{by}{Validation restricted to \code{NULL}. F2 currently only supports global tests.}

\item{parallel}{Number of parallel processes to use.}

\item{na.action}{Handling of missing values on the RHS.}

\item{strata}{Groups within which to constrain permutations.}

\item{bootstrap}{Logical; perform separate-sample residual bootstrapping in
dissimilarity space (Anderson 2017).}

\item{bias.adjust}{Logical; apply empirical bias correction to bootstrap variances.}

\item{...}{Other arguments passed to \code{vegdist}.}
}
\description{
Robust analysis of variance using distance matrices for partitioning variation
among sources. Unlike standard PERMANOVA, this function uses the F2 statistic
(Anderson 2017) which is robust to heterogeneous dispersions (Behrens-Fisher problem).
}
\details{
\code{F2_adonis2} implements the robust multivariate F2 statistic and the
matrix-based bootstrap procedure derived in Anderson et al. (2017).
}
\examples{
library(vegan)
# Example using the Norway Benthic Macrofauna Dataset (Anderson et al. 2017)
data(norway_pa)
data(norway_metadata)
set.seed(123)
fit_global <- F2_adonis2(norway_pa ~ Area, data = norway_metadata,
                         method = "jaccard", permutations = 999)
print(fit_global)

# Example with built-in data
data(dune)
data(dune.env)
F2_adonis2(dune ~ Management, data = dune.env, method = "bray")

}
\references{
Anderson, M. J., Walsh, D. C., Clarke, K. R., Gorley, R. N., & Guerra-Castro, E. (2017).
Some solutions to the multivariate Behrens–Fisher problem for dissimilarity-based
analyses. \emph{Australian & New Zealand Journal of Statistics}.
}
\seealso{
\code{\link[vegan]{adonis2}}, \code{\link[vegan]{betadisper}}
}
\author{
Lakviru Perera (based on logic by Marti J. Anderson)
}
